export default class RESPParser {
    private buffer: string;

    public constructor() {
        this.buffer = '';
    }

    public parse(data: string): (string | number | null | Error | (string | number | null | Error)[])[] {
        // TODO: Implement everything lol;
        
        return ['Something will be here very soon...'];
    }
}